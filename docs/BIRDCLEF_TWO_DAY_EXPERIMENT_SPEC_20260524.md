# BirdCLEF 2026 Two-Day Experiment Spec — 2026-05-24/25

Current confirmed best: **0.949**. Target: **0.960+**.

This spec answers the post-v612 concern: **yes, there are enough good ideas.** The failure mode to avoid is treating “no public replay slot” as “nothing to do.” For the next two days, the loop should always have at least one no-slot experiment running or queued.

Dates below use local PDT for work planning and UTC for Kaggle cap/submission accounting.

## Operating principles

1. **Keep the idea queue non-empty.** If source-safe public replay dries up, pivot immediately to repo-owned validation lanes.
2. **Separate no-slot validation from leaderboard spending.** Most work today/tomorrow should be private verifier kernels, local/trainer runs, export checks, source repairs, or blend audits.
3. **Spend Kaggle submission slots only on promoted candidates.** A promoted candidate needs hidden-safe output, row alignment, bounded displacement, and evidence beyond a single optimistic train-soundscape metric.
4. **Avoid exhausted families unless new artifacts appear.** Do not spend slots on EoS/PCEN/visual/HGNet scalar repeats, standalone clean-audio checkpoints, or public0952 clones.
5. **Use the neutral repo.** Active canonical clone: `/Users/yourslewis/.openclaw/repos/birdclef-2026`. Sync intentionally before trainer/GPU runs.

## Slot policy

### Remainder of 2026-05-24 UTC

Estimated state after v612: **2/5 used**, **3 slots remain**.

- Default: preserve all 3 unless a no-slot/private verifier passes promotion gates.
- Allowed slot spend today: at most **1** emergency/promoted candidate.
- Do not spend a slot on a candidate from a family that already tied/dropped unless it has a new artifact or materially different source signal.

### 2026-05-25 UTC

Expected reset: **5 slots available**.

- Budget: up to **2 promoted submissions**, keep **2–3 reserve slots** for late high-confidence discoveries.
- Preferred submissions if gates pass:
  1. Alexy NS1 hidden-safe sidecar blend.
  2. A genuinely improved G124/V2S-init sidecar or exportable SED/OOF sidecar.
- Never fill the day with near-duplicate scalar variants.

## Today: 2026-05-24 PDT / late 2026-05-24 UTC

Goal: set up the non-empty experiment machine and produce at least one concrete no-slot artifact before sleeping.

### T1 — Alexy NS1 sidecar extraction spec/skeleton

**Hypothesis:** Alexy NS1 has distinct CNN/noisy-student signal that may help as a low-weight sidecar, even though the direct source is below frontier and not slot-ready.

**Why today:** it is the most distinct fresh idea from the 22UTC scan and does not need GPU training to begin.

**Inputs:**

- Public source: `alexycactus/birdclef-2026-ns1-ensemble`
- Existing audit artifact: `artifacts/public_kernels_20260524_frontier_candidates/source_audit_20260524T2200Z_newleads/alexycactus__birdclef-2026-ns1-ensemble.json`

**Implementation task:**

Create a repo-owned hidden-safe branch producer:

```text
kaggle-kernels/v613-alexy-ns1-sidecar/
  script.py
  kernel-metadata.json
```

The script should produce branch diagnostics, not immediately claim final-slot readiness:

```text
submission_alexy_ns1_raw.csv
submission_anchor_raw.csv              # if anchor included
submission_before_alignment.csv
submission.csv                         # final guarded output only after branch checks
```

**First local checks:**

```bash
python3 -m py_compile kaggle-kernels/v613-alexy-ns1-sidecar/script.py
python3 -m json.tool kaggle-kernels/v613-alexy-ns1-sidecar/kernel-metadata.json >/dev/null
```

**No-slot/private Kaggle check:**

Push as a private verifier kernel only. Do **not** submit to the competition until promotion gates pass.

**Promotion gates:**

- Kernel COMPLETE, no traceback/failure markers.
- Branch output generated by rerunning inference on mounted rows, not copied public `(192,235)` output.
- `submission_alexy_ns1_raw.csv` finite, non-constant, row-aligned with anchor before final alignment.
- Low-weight rank blend with anchor has bounded displacement:
  - target corr vs anchor: `>=0.997`
  - target MAE: roughly `0.01–0.02`
- File/site bootstrap is positive with no catastrophic held-out group.

**Kill gates:**

- Any fixed sample fallback.
- Missing/non-public checkpoints with no reproducible alternative.
- Runtime too slow for hidden test.
- Branch only reproduces a known below-plateau final output.

**Expected result by end of today:**

- At minimum: skeleton + metadata + py_compile pass.
- Good outcome: private verifier pushed/running.
- Great outcome: private verifier COMPLETE with raw branch output ready for tomorrow’s anchored grid.

### T2 — Unified anchored sidecar validation harness manifest

**Hypothesis:** the bottleneck is not the lack of sidecars; it is inconsistent validation. A unified harness can keep slots safe and make tomorrow’s candidates comparable.

**Implementation task:**

Create a manifest or notes file listing every available sidecar output path/source and whether it is hidden-safe, public-output-only, train/dry-run-only, or rerunnable.

Suggested artifact:

```text
artifacts/anchored_blend_audit/sidecar_manifest_20260524.json
```

Initial members:

- Praxel HGNet raw branch.
- Samejima HGNet57 / v612 branch.
- Jungchan Model21 branch.
- StudyExchange S14 branch.
- Alexy NS1 branch once verifier output exists.

**Grid command template:**

```bash
python scripts/birdclef_public946_multi_sidecar_weight_grid.py \
  --base-csv <anchor.csv> \
  --sidecar prax_hgnet:<praxel_hgnet_raw.csv> \
  --sidecar sameji_hg57:<sameji_hg57_raw.csv> \
  --sidecar s14:<s14_raw.csv> \
  --sidecar jung21:<jungchan_subm_21.csv> \
  --labels-csv <train_soundscape_labels.csv> \
  --bootstrap-iters 1000 \
  --leave-one-group site \
  --output-json artifacts/anchored_blend_audit/sidecar_grid_20260524.json
```

**Promotion gates:**

- This harness alone cannot approve a submission; it is a rejection screen.
- Candidate must pass aggregate lift plus stable file/site bootstrap and independent source/runtime safety.

**Expected result by end of today:**

- Manifest exists.
- Missing outputs are clearly listed as blockers, not forgotten.
- Tomorrow’s first grid run has exact inputs.

### T3 — G124/V2S-init run prep

**Hypothesis:** the missing G124/S124 clue remains plausible, but scratch training was the wrong test. A V2S/external-init smoke is the next good experiment.

**Existing config:**

```text
configs/birdclef/g124_effv2s_public946_pseudo_smoke_20260523.json
```

**Prep task:**

Create a config copy for initialized smoke:

```text
configs/birdclef/g124_effv2s_public946_pseudo_smoke_20260524_v2sinit.json
```

Wire it to the best available V2S/external-pretrain checkpoint if present. Candidate source config:

```text
configs/birdclef/xc_v2s_q3_cap80_external_pretrain_balanced_ep12_bestloss.json
```

**Trainer sync note:**

Trainer venv has ML deps:

```bash
source ~/kaggle_envs/s6e3/bin/activate
```

But trainer `~/birdclef-2026` is not a git repo, so sync from the neutral repo intentionally before running.

**Expected result by end of today:**

- Config exists and parses.
- Required checkpoint path confirmed or blocker written down.
- Run command ready for tomorrow morning.

## Tomorrow: 2026-05-25 PDT / 2026-05-25 UTC

Goal: run at least two independent no-slot experiments and promote at most two candidates to leaderboard submissions if they pass gates.

### M1 — Finish Alexy NS1 verifier + sidecar grid

**Start condition:** T1 skeleton/private verifier exists.

**Run:**

1. Push/run private Kaggle verifier if not already complete.
2. Pull outputs/logs.
3. Verify raw branch shape/row IDs/finite/nonconstant.
4. Add Alexy branch to the unified sidecar harness.

**Artifacts:**

```text
artifacts/anchored_blend_audit/alexy_ns1_verify_20260525.json
artifacts/anchored_blend_audit/alexy_ns1_sidecar_grid_20260525.json
```

**Submit decision:**

- If all gates pass and displacement is bounded, create `v613` candidate.
- If gates fail, keep as idea-mining only and do not spend a slot.

### M2 — Run G124 EffV2-S V2S-init smoke on trainer

**Start condition:** T3 config exists and required init checkpoint is confirmed.

**Command:**

```bash
cd ~/birdclef-2026   # after intentional sync from neutral repo
source ~/kaggle_envs/s6e3/bin/activate
python scripts/birdclef_pseudolabel_student_train.py \
  --config configs/birdclef/g124_effv2s_public946_pseudo_smoke_20260524_v2sinit.json
```

**Artifacts:**

```text
artifacts/pseudolabels/students/g124-effv2s-public946-pseudo-smoke-20260524-v2sinit/
```

**Promotion gates:**

- Smoke val AUC at least `0.90–0.93`.
- Predictions are not degenerate and have useful correlation/diversity vs teacher.
- Blend audit finds useful sidecar weight `>=0.01` without unstable groups.
- Export/runtime smoke does not hang.

**Kill gates:**

- Scratch-like AUC, useless correlation, missing checkpoint, export hang, or no sidecar lift.

**Submit decision:**

- No same-day submission from smoke alone unless paired with a clean sidecar/export audit.
- If promising, queue pilot/full run rather than prematurely spending a slot.

### M3 — Real SED/export smoke fallback lane

**Hypothesis:** if Alexy/G124 stalls, real SED remains the highest-upside non-public-replay route.

**Existing smoke config:**

```text
configs/birdclef/sed_b0_5s_attn_smoke.json
```

**Command template:**

```bash
python scripts/birdclef_sed_smoke.py \
  --config configs/birdclef/sed_b0_5s_attn_smoke.json
```

If the smoke path requires the trainer/data mount, run after syncing repo to trainer and activating `~/kaggle_envs/s6e3`.

**Promotion gates:**

- Smoke trains and exports cleanly.
- Inference script can consume exported artifact.
- OOF/blend plan is clear before scaling.

**Kill gates:**

- Import/data path failures unresolved after one fix pass.
- Export path not viable.
- Runtime memory exceeds Kaggle-safe limits.

### A1 — Pseudo-label threshold/cache redesign

**Hypothesis:** past student runs often cloned the teacher. Improve the pseudo-label distribution before training more students.

**Command template:**

```bash
python scripts/birdclef_pseudolabel_threshold_sweep.py \
  --pred-npz <teacher_cache.npz> \
  --labels-csv <train_soundscape_labels.csv> \
  --output artifacts/pseudolabels/threshold_sweep_20260525.json
```

**Gate:**

- Class histogram remains sane.
- Rare species retained.
- Negative/positive balance avoids teacher-clone collapse.

### A2 — Per-class capped residual sidecar selector

**Hypothesis:** global sidecar weights tie because helpful classes are diluted by harmful classes. Per-class capped sidecar weights may improve reorderings without broad damage.

**Implementation sketch:**

Create an audit script or notebook artifact that learns capped per-class sidecar weights on matched train-soundscape labels only as a rejection screen.

**Hard constraints:**

- Weight cap per sidecar/class, e.g. `0.00–0.08`.
- L2/monotone regularization.
- Leave-file and leave-site validation required.
- No direct submission from train-only fit; it must inform fixed simple weights or prompt a better OOF setup.

**Artifact:**

```text
artifacts/anchored_blend_audit/per_class_residual_weights_20260525.json
```

### A3 — Source repair lane: WildSound or S14 branch

**WildSound:**

- Repair path issue offline only; public source errors on missing `/kaggle/input/birdclef-2026/train_metadata.csv`.
- Must avoid in-kernel training timeout.

**S14:**

- Extract as low-weight branch; direct expected score is below frontier.
- Useful if it adds stable sidecar movement in harness.

**Artifact:**

```text
artifacts/source_repairs/wildsound_v8_path_audit_20260525.md
artifacts/anchored_blend_audit/s14_low_weight_grid_20260525.json
```

## Decision tree

### If Alexy NS1 verifier passes

1. Run anchored sidecar grid.
2. If bounded + stable, package `v613`.
3. Spend one slot max.
4. If it ties/drops, do not keep nearby Alexy scalar variants unless a branch-level reason emerges.

### If Alexy fails but G124/V2S smoke is promising

1. Run export smoke.
2. Run sidecar blend audit.
3. Queue pilot/full training.
4. Do not submit smoke-only output.

### If both Alexy and G124 fail

1. Run SED/export smoke.
2. Run pseudo-label threshold redesign.
3. Continue source/artifact scout, but do not call the queue empty.

### If a fresh public source appears

1. Run the same source/output preflight as before.
2. Promote only if it is not a plateau family and has hidden-safe output or reproducible artifacts.
3. Otherwise add it to idea-mining or sidecar harness.

## Minimum deliverables by end of tomorrow

By the end of 2026-05-25 PDT, the repo should have at least:

1. Alexy NS1 sidecar decision: promoted, held, or killed with artifact evidence.
2. G124/V2S-init smoke result or a precise blocker.
3. Unified sidecar manifest/grid result.
4. At least one fallback lane started: SED/export smoke, pseudo-label threshold redesign, or per-class residual selector.
5. Updated `docs/BIRDCLEF_AUTORESEARCH_LOG.md` with outcomes and next candidates.

## Enough-ideas assessment

There are enough ideas for at least two days without touching weak public replays:

1. Alexy NS1 sidecar extraction.
2. G124 V2S-init reconstruction.
3. Unified anchored sidecar harness.
4. Per-class residual sidecar selector.
5. Real SED/export smoke.
6. Pseudo-label threshold/cache redesign.
7. Hard-negative/no-call residual gate.
8. External-data manifest refresh and short pretrain.
9. WildSound offline repair.
10. S14 branch extraction.
11. Export smoke matrix across best repo students.
12. OOF ensemble grid across repo-owned NPZs.

The correct next behavior is not “wait for better public kernels.” It is to keep this list moving through no-slot gates until one candidate earns a Kaggle slot.
