# BirdCLEF hill-climb ranked queue — 2026-06-01 16:20 UTC

## Live status
- UTC slots: **0/5 used** today (latest subs all 2026-05-31; ~7.6h to 00:00Z reset).
- Public best: **0.950** (v644 Yaroslav 0950 replay / v647 Ryuto EoS8 sidecar). Frontier E.
- v616 secondary baseline ~0.949.
- Trainer GPUs 0+1 free; no active local/trainer/Kaggle jobs.

## This run
- Built + DEV-gated a NEW datapoint: **3-backbone distill rank-stack** (ConvNeXt-w0.7 +
  ConvNeXt-w0.85 + RegNetY-008). Highest competence (weak-AUC 0.832) and highest DEV
  (0.00245) of any diversity stream yet, still orthogonal (decorr 0.737) — but blend
  weight pins 0.0, both q05=0 → **DEMOTE, gate_pass false**. Confirms 42/234 proxy
  ceiling, not model competence, is the binding limiter. No slot.

## Ranked queue by DEV potential (diversity-first)
1. **Hidden-safe 234-class distill package + late-window guarded LB probe** — the only
   honest way to value the orthogonal+competent distill family (proxy saturates at 0
   weight on 42/234 classes; v610 proves proxy is a two-way liar). Package the distill
   stream (or stack) as a real 234-class kernel; preflight; reserve for late UTC window
   (<3h to reset) if no DEV-passing candidate appears.
2. **Hunt a genuinely new accessible public birdclef-2026 foundation backbone** — only
   real, accessible slugs (no fabricated). Perch-v2 redundant; BirdNET orthogonal-XOR-
   competent (exhausted incl. geo-prior refuted); SurfPerch/AudioMAE absent. Keep scouting.
3. **Multi-teacher / stronger distill for a per-COLUMN win** — current distills beat E
   on 0/14 weak cols individually (macro artifact). A teacher ensemble might flip ≥1 col.
4. **BirdNET embedding-feature scoring** (not thresholded detections) — only remaining
   BirdNET angle; high effort.
5. TTA output-pool — DOWNGRADED redundant (winner already runs tta_shifts internally).

## STOP rule reaffirmed
No more shared-embedding head variants (PANNs/B0/DyMN10/no-call-gate/power-knob). Frozen.

## Slot decision this run
HOLD all 5 slots. Early UTC, no DEV-passing candidate, STOP rule forbids filler head
variants. Distill-stack is strongest diversity lead but its only legitimate use is a
late-window guarded 234-class LB probe — package first, probe in the <3h window.
