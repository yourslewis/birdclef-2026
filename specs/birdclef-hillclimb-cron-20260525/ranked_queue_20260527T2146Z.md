# Ranked queue — BirdCLEF hill-climb cron — 2026-05-27 21:46 UTC

## Live state
- Best known public LB: `0.949` (v616 and several public source fills tied before this run).
- Latest submitted refs: `53097345, 53097346, 53097347, 53097348, 53097349` pending.
- UTC daily slots after action: `5/5`; reset in ~`2.23h` at submission recount.

## Candidate ranking used this run

1. **Late source-code slot fill — ACCEPTED / EXECUTED**
   - Reason: no positive v616-sidecar repo candidate was ready, and late-day policy says use valid exploratory slots.
   - Submitted: v626 Jaejohn Perch meta-probe, v627 Hideyukizushi ProtoSSM, v628 Cliff gate combo, v629 Yaroslav BirdNET third, v630 Tucker distilled SED.
2. **PANNs/Cnn14 all-class no-file hidden-test package — NEXT repo-owned action**
   - Best current train_soundscape row metric (`0.647816`) and strong no-train/non-Aves metrics, but proxy sidecar lift vs v616 was negative; needs true hidden-safe package/eval rather than another OOF proxy sidecar.
3. **Fused DyMN10+PANNs file-MIL clue — REVISE**
   - Best file-MIL among sequence heads (`0.675982`) but row metric trails PANNs and sidecar lift is still negative.
4. **Soundscape-native B0 all-class — KEEP DATA POINT / REVISE**
   - File-MIL close to best (`0.673756`) but pooled calibration poor; not a direct package.
5. **No-call/acoustic-background protocol — NEXT distinct data branch if slots capped**
   - Still under-mined; should start with trusted negative/no-call audit before model training.

## Verifier notes
- Rejected direct sequence sidecars because all recent 234-class sidecar audits had lift vs v616 `<0`.
- Source submissions were not duplicate descriptions and not exact duplicate public dry-run hashes of v621-v625.
- No static/fallback CSV was uploaded; Kaggle code submission API will rerun each selected source.
